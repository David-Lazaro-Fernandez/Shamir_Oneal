import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'shamir_oneal',
  webDir: 'dist/shamir_oneal',
  bundledWebRuntime: false
};

export default config;
